spring:
  datasource:
    url: jdbc:h2:mem:shippingdb;DB_CLOSE_DELAY=-1;MODE=MySQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect

application:
  security:
    jwt:
      secret-key: testSecretKeyForJWTTokenTestingPurposes1234567890

ghn:
  api:
    url: https://dev-online-gateway.ghn.vn/shiip/public-api/v2
    token: test-token
    shopId: "123456"
  webhook:
    secret-key: test-secret
