# Plan: 03-01 - Xây dựng Endpoint nhận IPN (Webhook) và Callback từ VNPay

**Wave:** 1
**Depends on:** 02-02
**Autonomous:** true
**Files modified:**
- BE/payment-service/src/main/java/com/medcare/paymentservice/controller/PaymentController.java
- BE/payment-service/src/main/java/com/medcare/paymentservice/service/PaymentService.java
- BE/payment-service/src/main/java/com/medcare/paymentservice/entity/PaymentLog.java
- BE/payment-service/src/main/java/com/medcare/paymentservice/repository/PaymentLogRepository.java

## Goal
Xử lý thông báo thanh toán từ VNPay một cách an toàn và tin cậy thông qua IPN và Callback.

## Tasks

<task>
<action>
Tạo Entity `PaymentLog.java` để lưu trữ dữ liệu thô (raw) từ VNPay gửi về.
- Fields: `paymentId`, `rawResponse`, `createdAt`.
</action>
<acceptance_criteria>
- Entity được tạo đúng mapping với database.
</acceptance_criteria>
</task>

<task>
<action>
Tạo `PaymentLogRepository.java`.
</action>
<acceptance_criteria>
- Repository sẵn sàng sử dụng.
</acceptance_criteria>
</task>

<task>
<action>
Bổ sung logic xử lý IPN trong `PaymentService.java`.
- Phương thức `processIpn(Map<String, String> params)`.
- Thực hiện 5 bước kiểm tra: Chữ ký, Đơn hàng, Số tiền, Trạng thái xử lý.
- Lưu log vào `PaymentLog`.
</action>
<acceptance_criteria>
- Logic xử lý trả về Map chứa `RspCode` và `Message`.
</acceptance_criteria>
</task>

<task>
<action>
Bổ sung Endpoint trong `PaymentController.java`.
- `GET /api/payment/vnpay-ipn`: Nhận thông báo từ VNPay.
- `GET /api/payment/vnpay-callback`: Nhận chuyển hướng người dùng (Redirect).
</action>
<acceptance_criteria>
- Các endpoint nhận tham số qua URL và gọi service xử lý.
</acceptance_criteria>
</task>

## Verification
- Sử dụng công cụ giả lập IPN hoặc gửi request cURL mô phỏng cấu trúc VNPay (cần tính toán đúng chữ ký hash để pass validation).
- Kiểm tra bảng `payments` và `payment_logs` sau khi gọi IPN.
