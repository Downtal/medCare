# Plan: 03-02 - Kết nối payment-service với order-service qua OpenFeign

**Wave:** 2
**Depends on:** 03-01
**Autonomous:** true
**Files modified:**
- BE/payment-service/src/main/java/com/medcare/paymentservice/client/OrderClient.java
- BE/order-service/src/main/java/com/medcare/orderservice/controller/OrderController.java
- BE/order-service/src/main/java/com/medcare/orderservice/service/OrderService.java

## Goal
Đồng bộ trạng thái đơn hàng ngay khi thanh toán thành công.

## Tasks

<task>
<action>
Tạo `OrderClient.java` trong `payment-service` sử dụng OpenFeign.
- Interface gọi đến `order-service`.
- Phương thức `updatePaymentStatus(String orderCode, String status)`.
</action>
<acceptance_criteria>
- Feign Client được khai báo đúng annotation `@FeignClient(name = "order-service")`.
</acceptance_criteria>
</task>

<task>
<action>
Bổ sung Endpoint trong `order-service/OrderController.java`.
- `PUT /api/orders/internal/{orderCode}/payment-status`.
- Tham số: `status` (PAID, FAILED).
</action>
<acceptance_criteria>
- Endpoint internal có thể được gọi từ các service khác.
</acceptance_criteria>
</task>

<task>
<action>
Cập nhật `PaymentService.java` (payment-service) để gọi `OrderClient` khi IPN thành công.
</action>
<acceptance_criteria>
- Sau khi xác thực IPN thành công, trạng thái Order được cập nhật.
</acceptance_criteria>
</task>

## Verification
- Chạy đồng thời `order-service` và `payment-service`.
- Giả lập IPN thành công cho một `orderCode` có sẵn.
- Kiểm tra bảng `orders` trong `medcare_order_db` xem status có chuyển sang `CONFIRMED` hoặc `PAID` không.
