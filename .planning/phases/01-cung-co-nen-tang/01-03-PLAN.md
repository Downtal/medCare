# Plan: 01-03 - Tái cấu trúc các service còn lại

**Wave:** 3
**Depends on:** 01-01, 01-02
**Autonomous:** true
**Files modified:**
- BE/order-service/build.gradle
- BE/shipping-service/build.gradle
- BE/review-service/build.gradle
- BE/promotion-service/build.gradle
- BE/inventory-service/build.gradle
- (Các file config tương ứng trong mỗi service)

## Goal
Hoàn tất việc áp dụng `common-lib` cho toàn bộ hệ thống microservices.

## Tasks

<task>
<action>
Cập nhật `build.gradle` cho tất cả các service còn lại:
- Thêm `implementation project(':common-lib')`.
- Dọn dẹp các dependency thừa.
</action>
<acceptance_criteria>
- Tất cả các module đều phụ thuộc vào `common-lib`.
</acceptance_criteria>
</task>

<task>
<action>
Thay thế `JwtAuthenticationFilter` và cấu hình Redis cục bộ tại:
- `order-service`
- `shipping-service`
- `review-service`
- `promotion-service`
- `inventory-service`
Sử dụng Shared beans từ `common-lib`.
</action>
<acceptance_criteria>
- Không còn code lặp lại về JWT/Redis tại các service này.
- Tất cả các service khởi động thành công.
</acceptance_criteria>
</task>

<task>
<action>
Thiết lập Global Exception Handler dùng chung từ `common-lib` (nếu chưa có).
- Đảm bảo trả về mã lỗi 401 Unauthorized khi JWT lỗi thay vì lỗi 500.
</action>
<acceptance_criteria>
- Response khi lỗi JWT có format nhất quán.
- HTTP Status Code là 401.
</acceptance_criteria>
</task>

## Verification
- Kiểm tra kết nối Redis của `order-service` (Cart flow).
- Kiểm tra quyền truy cập API của `shipping-service`.
- Chạy `./gradlew build` toàn bộ dự án BE.
