# Plan: 01-02 - Tái cấu trúc user-service và product-service

**Wave:** 2
**Depends on:** 01-01
**Autonomous:** true
**Files modified:**
- BE/user-service/build.gradle
- BE/user-service/src/main/java/com/medcare/userservice/config/SecurityConfig.java
- BE/product-service/build.gradle
- BE/product-service/src/main/java/com/medcare/productservice/config/RedisConfig.java
- BE/product-service/src/main/java/com/medcare/productservice/config/SecurityConfig.java

## Goal
Thay thế các cấu hình cục bộ trong `user-service` và `product-service` bằng các thành phần từ `common-lib`.

## Tasks

<task>
<read_first>
- BE/user-service/build.gradle
</read_first>
<action>
Cập nhật `build.gradle` của `user-service`:
- Thêm `implementation project(':common-lib')`.
- Loại bỏ các dependency đã có trong `common-lib` (jjwt, redis-starter, etc.) để tránh xung đột.
</action>
<acceptance_criteria>
- build.gradle có dependency `common-lib`.
- Không còn các dependency bị trùng lặp.
</acceptance_criteria>
</task>

<task>
<read_first>
- BE/user-service/src/main/java/com/medcare/userservice/config/SecurityConfig.java
</read_first>
<action>
Refactor `SecurityConfig.java` trong `user-service`:
- Loại bỏ inner class `JwtAuthenticationFilter`.
- Inject `JwtAuthenticationFilter` (từ `common-lib`) vào `SecurityFilterChain`.
- Sử dụng `@Value` để map `secretKey` nếu cần, hoặc cấu hình `JwtService` để tự đọc.
</action>
<acceptance_criteria>
- `SecurityConfig` gọn gàng hơn, không còn logic parse JWT thủ công.
- Ứng dụng khởi động được.
</acceptance_criteria>
</task>

<task>
<read_first>
- BE/product-service/build.gradle
</read_first>
<action>
Tương tự, cập nhật `build.gradle` của `product-service` để phụ thuộc vào `common-lib`.
</action>
<acceptance_criteria>
- build.gradle có dependency `common-lib`.
</acceptance_criteria>
</task>

<task>
<read_first>
- BE/product-service/src/main/java/com/medcare/productservice/config/RedisConfig.java
</read_first>
<action>
Xóa file `RedisConfig.java` cục bộ trong `product-service` (hoặc giữ lại và đánh dấu `@Primary` nếu cần tùy biến sâu, nhưng ưu tiên dùng mặc định từ `common-lib`).
- Đảm bảo service vẫn nhận được `RedisTemplate` qua Auto-configuration.
</action>
<acceptance_criteria>
- File cục bộ được xóa hoặc refactor để sử dụng Shared config.
- Log khởi động hiển thị kết nối Redis thành công.
</acceptance_criteria>
</task>

## Verification
- Chạy `./gradlew :user-service:bootRun` và kiểm tra đăng nhập (nếu có thể).
- Chạy `./gradlew :product-service:bootRun` và kiểm tra truy xuất dữ liệu có cache.
