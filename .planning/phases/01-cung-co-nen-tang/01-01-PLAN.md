# Plan: 01-01 - Xây dựng nền tảng common-lib

**Wave:** 1
**Depends on:** None
**Autonomous:** true
**Files modified:**
- BE/common-lib/build.gradle
- BE/common-lib/src/main/java/com/medcare/common/config/SharedRedisConfig.java
- BE/common-lib/src/main/java/com/medcare/common/security/JwtService.java
- BE/common-lib/src/main/java/com/medcare/common/security/JwtAuthenticationFilter.java
- BE/common-lib/src/main/resources/META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports

## Goal
Thiết lập module `common-lib` chứa các cấu hình và logic dùng chung cho Redis và JWT, sử dụng cơ chế Auto-configuration của Spring Boot.

## Tasks

<task>
<read_first>
- BE/common-lib/build.gradle
- BE/product-service/src/main/java/com/medcare/productservice/config/RedisConfig.java
</read_first>
<action>
Cập nhật `BE/common-lib/build.gradle` để thêm các dependency cần thiết: 
- `spring-boot-starter-data-redis`
- `spring-boot-starter-security`
- `io.jsonwebtoken:jjwt-api:0.12.6` (và runtime impl/jackson)
- `com.fasterxml.jackson.datatype:jackson-datatype-jsr310`
</action>
<acceptance_criteria>
- build.gradle chứa đầy đủ các dependency trên.
- Module common-lib có thể build thành công (./gradlew :common-lib:build).
</acceptance_criteria>
</task>

<task>
<read_first>
- BE/product-service/src/main/java/com/medcare/productservice/config/RedisConfig.java
</read_first>
<action>
Tạo `SharedRedisConfig.java` trong `com.medcare.common.config` của `common-lib`.
- Di chuyển logic `ObjectMapper` (với `JavaTimeModule` và `DefaultTyping`) vào đây.
- Định nghĩa Bean `RedisTemplate<String, Object>` dùng `GenericJackson2JsonRedisSerializer`.
- Định nghĩa Bean `RedisCacheManager` mặc định.
</action>
<acceptance_criteria>
- `SharedRedisConfig.java` tồn tại và không có lỗi syntax.
- Sử dụng đúng `GenericJackson2JsonRedisSerializer`.
</acceptance_criteria>
</task>

<task>
<read_first>
- BE/user-service/src/main/java/com/medcare/userservice/config/SecurityConfig.java
</read_first>
<action>
Tạo `JwtService.java` và `JwtAuthenticationFilter.java` trong `com.medcare.common.security` của `common-lib`.
- `JwtService` đảm nhận việc parse token và validate.
- `JwtAuthenticationFilter` hỗ trợ parse Token và fallback Gateway Headers (`X-User-Id`, `X-User-Role`).
- Thêm xử lý lỗi 401 rõ ràng khi Token không hợp lệ thay vì chỉ clear context.
</action>
<acceptance_criteria>
- Hai class trên được tạo thành công.
- Logic parse JWT khớp với pattern cũ nhưng được tối ưu hóa.
</acceptance_criteria>
</task>

<task>
<action>
Tạo file `src/main/resources/META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` trong `common-lib`.
- Khai báo `com.medcare.common.config.SharedRedisConfig`.
- Khai báo `com.medcare.common.security.JwtService`.
</action>
<acceptance_criteria>
- File tồn tại với đúng đường dẫn.
- Chứa tên đầy đủ của các class cấu hình.
</acceptance_criteria>
</task>

## Verification
- Chạy lệnh `./gradlew :common-lib:build` để đảm bảo module không lỗi.
- Kiểm tra file `.imports` đã được đóng gói vào file jar hay chưa.
