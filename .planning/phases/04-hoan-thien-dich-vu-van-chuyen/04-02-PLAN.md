# Plan: 04-02 - Đồng bộ dữ liệu địa chỉ hành chính từ GHN API

**Wave:** 2
**Depends on:** 04-01
**Autonomous:** true
**Files modified:**
- BE/shipping-service/src/main/java/com/medcare/shippingservice/client/GHNClient.java
- BE/shipping-service/src/main/java/com/medcare/shippingservice/service/MasterDataService.java
- BE/shipping-service/src/main/java/com/medcare/shippingservice/controller/MasterDataController.java
- BE/user-service/src/main/java/com/medcare/userservice/entity/Address.java
- BE/user-service/src/main/java/com/medcare/userservice/dto/AddressDto.java

## Goal
Xây dựng API để Frontend lấy danh sách Tỉnh/Huyện/Xã chính xác từ GHN, và cập nhật User Service để lưu trữ các mã địa chỉ này, phục vụ cho việc tạo đơn hàng.

## Tasks

<task>
<action>
Tạo `GHNClient.java` (Feign Client) trong `shipping-service` để gọi API của GHN.
- Cấu hình URL và Header (`Token`).
- Các phương thức: `getProvinces()`, `getDistricts(provinceId)`, `getWards(districtId)`.
</action>
<acceptance_criteria>
- Client gọi thành công API thật của GHN.
</acceptance_criteria>
</task>

<task>
<action>
Xây dựng `MasterDataService` và `MasterDataController` trong `shipping-service`.
- Cung cấp các REST endpoint công khai cho Frontend: `GET /api/shipping/provinces`, v.v.
- (Tùy chọn) Thêm logic caching bằng Redis để tránh gọi quá nhiều tới GHN.
</action>
<acceptance_criteria>
- Các endpoint trả về JSON dữ liệu địa chỉ.
</acceptance_criteria>
</task>

<task>
<action>
Cập nhật `Address` và `AddressDto` trong `user-service`.
- Thêm hoặc đảm bảo các trường `cityId` (int), `districtId` (int), `wardCode` (String) tồn tại.
- Cập nhật logic thêm/sửa địa chỉ để bắt buộc Frontend phải gửi các trường này.
</action>
<acceptance_criteria>
- Khi tạo mới địa chỉ, các mã vùng được lưu chính xác vào DB.
</acceptance_criteria>
</task>

<task>
<action>
Cập nhật `OrderRequest` trong `order-service`.
- Bổ sung `cityId`, `districtId`, `wardCode` vào thông tin Checkout.
- Logic tạo `Order` sẽ lưu lại các giá trị này (vào cột bổ sung hoặc chuỗi JSON).
</action>
<acceptance_criteria>
- Order được tạo có đủ dữ liệu để truyền qua `shipping-service` thực hiện tạo vận đơn GHN.
</acceptance_criteria>
</task>

## Verification
- Gọi API `/api/shipping/provinces` qua Postman, kiểm tra kết quả có chứa dữ liệu từ GHN.
- Thực hiện luồng lưu địa chỉ mới bên User Service có kèm các mã ID.
