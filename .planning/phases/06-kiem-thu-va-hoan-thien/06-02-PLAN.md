# Plan: 06-02 - Hoàn thiện API và Giao diện Tracking/Voucher

**Wave:** 2
**Depends on:** 06-01
**Autonomous:** true
**Files modified:**
- BE/shipping-service/src/main/java/com/medcare/shippingservice/service/ShippingService.java
- BE/shipping-service/src/main/java/com/medcare/shippingservice/controller/ShippingController.java
- BE/order-service/src/main/java/com/medcare/orderservice/dto/OrderDetailResponse.java
- BE/order-service/src/main/java/com/medcare/orderservice/service/OrderService.java
- FE/src/components/order/OrderTimeline.tsx (Giả định file trên Frontend)

## Goal
Cung cấp dữ liệu lộ trình đơn hàng chi tiết từ GHN và hiển thị thông tin ưu đãi Voucher đầy đủ cho người dùng.

## Tasks

<task>
<action>
Cập nhật `ShippingService.java` để lấy thông tin chi tiết từ GHN.
- Viết hàm `getTrackingHistory(String trackingCode)`.
- Gọi API GHN `v2/shipping-order/detail` để lấy mảng `logs`.
</action>
<read_first>
- BE/shipping-service/src/main/java/com/medcare/shippingservice/service/ShippingService.java
</read_first>
<acceptance_criteria>
- Hàm trả về danh sách lịch sử trạng thái của GHN.
</acceptance_criteria>
</task>

<task>
<action>
Mở API trong `ShippingController.java`.
- `GET /api/shipping/tracking/{trackingCode}/history`
</action>
<acceptance_criteria>
- Endpoint trả về JSON history thành công.
</acceptance_criteria>
</task>

<task>
<action>
Cập nhật `order-service` để trả về đủ dữ liệu.
- Tạo `OrderDetailResponse` DTO chứa đầy đủ: Order info, Items, Voucher info (Code, Discount), Shipping info (Tracking code, provider).
- Trong `OrderService`, tích hợp gọi sang `shipping-service` để lấy lịch sử tracking nếu cần (hoặc để Frontend gọi trực tiếp). Quyết định: Frontend gọi trực tiếp `shipping-service` cho history để giảm tải cho `order-service`.
</action>
<read_first>
- BE/order-service/src/main/java/com/medcare/orderservice/controller/OrderController.java
</read_first>
<acceptance_criteria>
- API GET Order detail trả về đủ thông tin Voucher.
</acceptance_criteria>
</task>

<task>
<action>
(Frontend) Cập nhật giao diện chi tiết đơn hàng.
- Thêm component hiển thị Timeline dựa trên dữ liệu từ `shipping-service`.
- Hiển thị Voucher Discount trong phần thanh toán.
</action>
<acceptance_criteria>
- Người dùng thấy được sơ đồ vận chuyển trực quan.
</acceptance_criteria>
</task>

## Verification
- Truy cập trang chi tiết đơn hàng trên web.
- Kiểm tra Timeline hiển thị đúng các mốc thời gian từ GHN.
- Kiểm tra số tiền Voucher giảm giá hiển thị khớp với DB.
