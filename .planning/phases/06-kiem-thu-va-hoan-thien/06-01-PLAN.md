# Plan: 06-01 - Đồng bộ tồn kho thực tế giữa Order và Inventory

**Wave:** 1
**Depends on:** None
**Autonomous:** true
**Files modified:**
- BE/inventory-service/src/main/java/com/medcare/inventoryservice/dto/StockDeductRequest.java
- BE/inventory-service/src/main/java/com/medcare/inventoryservice/service/InventoryService.java
- BE/inventory-service/src/main/java/com/medcare/inventoryservice/controller/InventoryController.java
- BE/order-service/src/main/java/com/medcare/orderservice/client/InventoryClient.java
- BE/order-service/src/main/java/com/medcare/orderservice/service/OrderService.java

## Goal
Triển khai cơ chế trừ tồn kho thực tế khi có đơn hàng mới thông qua gọi đồng bộ giữa Order Service và Inventory Service.

## Tasks

<task>
<action>
Tạo `StockDeductRequest.java` trong `inventory-service`.
- Chứa danh sách các item cần trừ: `productId`, `quantity`.
</action>
<read_first>
- BE/inventory-service/src/main/java/com/medcare/inventoryservice/dto/InventoryImportRequest.java
</read_first>
<acceptance_criteria>
- DTO được tạo thành công với Lombok.
</acceptance_criteria>
</task>

<task>
<action>
Cập nhật `InventoryService.java` để xử lý trừ kho.
- Viết hàm `deductStock(StockDeductRequest request)`.
- Duyệt qua từng item:
  - Tìm các lô (batches) của `productId` sắp xếp theo `expiryDate` tăng dần (FIFO).
  - Trừ dần `quantityAvailable` của các lô cho đến khi đủ số lượng yêu cầu.
  - Nếu duyệt hết các lô mà vẫn không đủ -> Ném lỗi `InsufficientStockException`.
- Ghi log vào `InventoryLog` với type `OUT`.
</action>
<read_first>
- BE/inventory-service/src/main/java/com/medcare/inventoryservice/service/InventoryService.java
- BE/inventory-service/src/main/java/com/medcare/inventoryservice/repository/InventoryBatchRepository.java
</read_first>
<acceptance_criteria>
- Logic trừ kho hoạt động đúng theo nguyên tắc lô hết hạn trước trừ trước.
- Ném lỗi chính xác khi thiếu hàng.
</acceptance_criteria>
</task>

<task>
<action>
Mở endpoint trong `InventoryController.java`.
- `POST /api/inventory/internal/deduct`
</action>
<read_first>
- BE/inventory-service/src/main/java/com/medcare/inventoryservice/controller/InventoryController.java
</read_first>
<acceptance_criteria>
- Endpoint hoạt động.
</acceptance_criteria>
</task>

<task>
<action>
Tạo `InventoryClient.java` trong `order-service` và tích hợp vào `OrderService`.
- Khai báo FeignClient gọi sang `inventory-service`.
- Trong `OrderService.createOrder()`, thay thế logic mock `mockStockCheck` bằng gọi `inventoryClient.deductStock()`.
- Chú ý: Gọi trừ kho TRƯỚC KHI kết thúc transaction tạo đơn hàng để đảm bảo tính nhất quán.
</action>
<read_first>
- BE/order-service/src/main/java/com/medcare/orderservice/service/OrderService.java
</read_first>
<acceptance_criteria>
- `OrderService` gọi thành công `inventory-service`.
- Nếu `inventory-service` báo lỗi, transaction tạo đơn hàng bị rollback.
</acceptance_criteria>
</task>

## Verification
- Tạo đơn hàng qua Postman.
- Kiểm tra DB `medcare_inventory_db` bảng `inventory_batches` xem số lượng có giảm không.
- Kiểm tra bảng `inventory_logs` xem có dòng `OUT` không.
