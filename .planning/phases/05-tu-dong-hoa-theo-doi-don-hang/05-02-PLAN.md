# Plan: 05-02 - Đồng bộ trạng thái vào Order Service

**Wave:** 2
**Depends on:** 05-01
**Autonomous:** true
**Files modified:**
- BE/order-service/src/main/java/com/medcare/orderservice/controller/OrderController.java
- BE/order-service/src/main/java/com/medcare/orderservice/service/OrderService.java
- BE/order-service/src/main/java/com/medcare/orderservice/dto/OrderStatusUpdateRequest.java

## Goal
Xây dựng Endpoint nội bộ trong `order-service` để nhận trạng thái mới từ `shipping-service` và cập nhật entity `Order`.

## Tasks

<task>
<action>
Tạo `OrderStatusUpdateRequest.java` trong thư mục `dto`.
- Có thuộc tính `status` (String hoặc Enum).
</action>
<read_first>
- BE/order-service/src/main/java/com/medcare/orderservice/dto/OrderRequest.java
</read_first>
<acceptance_criteria>
- Class DTO được tạo thành công với annotations của Lombok.
</acceptance_criteria>
</task>

<task>
<action>
Cập nhật `OrderService.java`.
- Thêm phương thức `updateOrderStatusInternal(String orderCode, String newStatus)`.
- Ánh xạ string status sang enum `OrderStatus`. Nếu `SHIPPING`, cập nhật thành `SHIPPING`. Nếu `DELIVERED`, cập nhật thành `DELIVERED`.
- Lưu vào DB.
</action>
<read_first>
- BE/order-service/src/main/java/com/medcare/orderservice/service/OrderService.java
</read_first>
<acceptance_criteria>
- Trạng thái đơn hàng thay đổi đúng theo giá trị truyền vào.
</acceptance_criteria>
</task>

<task>
<action>
Cập nhật `OrderController.java`.
- Mở một endpoint `PUT /api/orders/internal/{orderCode}/status`.
- RequestBody là `OrderStatusUpdateRequest`.
- Gọi hàm của `OrderService`.
</action>
<read_first>
- BE/order-service/src/main/java/com/medcare/orderservice/controller/OrderController.java
</read_first>
<acceptance_criteria>
- Endpoint hoạt động và trả về HTTP 200.
</acceptance_criteria>
</task>

## Verification
- Chạy toàn bộ luồng: Postman gửi Webhook giả đến `shipping-service`, `shipping-service` xử lý rồi gọi OpenFeign sang `order-service`.
- Mở Database `medcare_order_db` kiểm tra xem cột `status` của bảng `orders` có được cập nhật thành `SHIPPING` không.
