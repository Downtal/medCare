# Plan: 02-02 - Tích hợp cấu hình VNPay và logic tạo Payment URL

**Wave:** 2
**Depends on:** 02-01
**Autonomous:** true
**Files modified:**
- BE/payment-service/src/main/java/com/medcare/paymentservice/config/VNPayConfig.java
- BE/payment-service/src/main/java/com/medcare/paymentservice/util/VNPayUtil.java
- BE/payment-service/src/main/java/com/medcare/paymentservice/service/PaymentService.java
- BE/payment-service/src/main/java/com/medcare/paymentservice/controller/PaymentController.java
- BE/payment-service/src/main/java/com/medcare/paymentservice/dto/PaymentRequest.java
- BE/payment-service/src/main/java/com/medcare/paymentservice/dto/PaymentResponse.java

## Goal
Xây dựng logic tạo URL thanh toán VNPay dựa trên thông tin từ Order Service.

## Tasks

<task>
<action>
Tạo `VNPayConfig.java` để đọc các thông số từ biến môi trường/application.yml.
- `vnp_TmnCode`
- `vnp_HashSecret`
- `vnp_PayUrl`
- `vnp_ReturnUrl`
</action>
<acceptance_criteria>
- Class Config chứa đầy đủ các tham số cần thiết.
</acceptance_criteria>
</task>

<task>
<action>
Tạo `VNPayUtil.java` chứa các hàm bổ trợ:
- `hmacSHA512(key, data)`: Tạo chữ ký.
- `hashAllFields(Map fields)`: Sắp xếp và nối chuỗi tham số.
- `getIpAddress(HttpServletRequest)`: Lấy IP client.
</action>
<acceptance_criteria>
- Util class chứa logic hash đúng chuẩn VNPay.
</acceptance_criteria>
</task>

<task>
<action>
Xây dựng `PaymentService.java` với phương thức `createPaymentUrl`.
- Tính toán số tiền (* 100).
- Tạo mã tham chiếu giao dịch.
- Gọi `VNPayUtil` để tạo URL.
- (Tùy chọn) Lưu thông tin `PENDING` vào bảng `payments`.
</action>
<acceptance_criteria>
- Phương thức trả về một URL bắt đầu bằng `https://sandbox.vnpayment.vn/paymentv2/vpcpay.html`.
</acceptance_criteria>
</task>

<task>
<action>
Tạo `PaymentController.java` với endpoint `POST /api/payment/create`.
- Nhận `PaymentRequest` (orderId, amount, description).
- Trả về `PaymentResponse` chứa URL thanh toán.
</action>
<acceptance_criteria>
- Endpoint hoạt động và trả về mã 200 kèm URL.
</acceptance_criteria>
</task>

## Verification
- Sử dụng Postman/cURL để gọi API tạo URL.
- Copy URL dán vào trình duyệt để kiểm tra xem có hiện trang thanh toán của VNPay Sandbox hay không.
