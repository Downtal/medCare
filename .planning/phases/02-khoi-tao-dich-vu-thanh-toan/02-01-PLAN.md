# Plan: 02-01 - Khởi tạo dự án payment-service

**Wave:** 1
**Depends on:** None
**Autonomous:** true
**Files modified:**
- BE/settings.gradle
- BE/payment-service/build.gradle
- BE/payment-service/src/main/resources/application.yml
- BE/payment-service/src/main/resources/bootstrap.yml
- BE/payment-service/src/main/java/com/medcare/paymentservice/PaymentServiceApplication.java

## Goal
Khởi tạo cấu trúc dự án `payment-service`, thiết lập dependency và đăng ký với hệ thống microservices.

## Tasks

<task>
<read_first>
- BE/settings.gradle
</read_first>
<action>
Thêm module `:payment-service` vào `BE/settings.gradle`.
</action>
<acceptance_criteria>
- `settings.gradle` bao gồm `include 'payment-service'`.
</acceptance_criteria>
</task>

<task>
<action>
Tạo file `BE/payment-service/build.gradle` với các dependency:
- `common-lib`
- `spring-boot-starter-web`
- `spring-boot-starter-data-jpa`
- `spring-cloud-starter-netflix-eureka-client`
- `spring-cloud-starter-openfeign`
- `mysql-connector-j`
</action>
<acceptance_criteria>
- `build.gradle` được tạo với đúng dependency và phiên bản.
</acceptance_criteria>
</task>

<task>
<action>
Tạo `PaymentServiceApplication.java` trong `com.medcare.paymentservice`.
- Thêm các annotation: `@SpringBootApplication`, `@EnableDiscoveryClient`, `@EnableFeignClients`.
</action>
<acceptance_criteria>
- Main class được khởi tạo và không lỗi syntax.
</acceptance_criteria>
</task>

<task>
<action>
Tạo file `application.yml` và `bootstrap.yml` trong `src/main/resources`.
- Cấu hình port (8089).
- Cấu hình kết nối MySQL (`medcare_payment_db`).
- Cấu hình Eureka client.
</action>
<acceptance_criteria>
- File cấu hình đầy đủ thông tin port, db, eureka.
</acceptance_criteria>
</task>

## Verification
- Chạy `./gradlew :payment-service:classes` để kiểm tra biên dịch.
- (Nếu có thể) Chạy service và kiểm tra trên Eureka dashboard (localhost:8761).
